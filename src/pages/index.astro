---
import { getProjectIndex } from '../lib/docs-data';

const projects = await getProjectIndex();
const fallback = projects[0]?.slug;

if (!fallback) {
  throw new Error('No projects available for docs redirect.');
}

return Astro.redirect(`/${fallback}/`);
---
