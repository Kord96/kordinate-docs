{
  "project": "bookstore-apis",
  "generated": "2026-03-28",
  "purpose": "API test automation framework validating the FakeRestAPI Bookstore REST endpoints for Books and Authors using data-driven CRUD tests with Allure reporting.",
  "stack": {
    "languages": [
      "Java 21"
    ],
    "frameworks": [
      "RestAssured 5.5.1",
      "TestNG 7.11.0",
      "Allure 2.29.1",
      "Lombok 1.18.38",
      "Jackson 2.19.0",
      "Maven 3.x"
    ],
    "runtime": "JVM 21 (Zulu distribution in CI)"
  },
  "groups": [
    {
      "id": "test-execution",
      "name": "Test Execution",
      "description": "Test classes and the runner configuration that orchestrate the suite"
    },
    {
      "id": "support-libraries",
      "name": "Support Libraries",
      "description": "Utility layers for HTTP, data loading, configuration, and domain models"
    },
    {
      "id": "test-data",
      "name": "Test Data",
      "description": "JSON fixtures and configuration files that drive test inputs"
    },
    {
      "id": "ci-cd",
      "name": "CI / CD",
      "description": "Automated build, test, and reporting pipeline"
    },
    {
      "id": "external",
      "name": "External",
      "description": "Systems outside the project boundary"
    },
    {
      "id": "actors",
      "name": "Actors",
      "description": "People and automated triggers that interact with the system"
    }
  ],
  "nodes": [
    {
      "id": "qa-engineer",
      "name": "QA Engineer",
      "type": "actor",
      "group": "actors",
      "description": "Writes, maintains, and runs the test suite locally via Maven or IDE"
    },
    {
      "id": "ci-system",
      "name": "GitHub Actions",
      "type": "actor",
      "group": "actors",
      "description": "Triggered on every push, runs the full suite and publishes Allure reports to gh-pages"
    },
    {
      "id": "test-suites",
      "name": "Test Suites",
      "type": "library",
      "group": "test-execution",
      "description": "TestNG test classes containing 23 test methods (11 Book, 12 Author) covering CRUD operations, negative cases, and unsupported method validation",
      "file": "src/test/java/bookstore/tests/",
      "patterns": [
        {
          "name": "data-driven-tests",
          "category": "testing"
        },
        {
          "name": "arrange-act-assert",
          "category": "testing"
        },
        {
          "name": "step-annotated-reporting",
          "category": "observability"
        },
        {
          "name": "known-issue-tagging",
          "category": "testing"
        }
      ]
    },
    {
      "id": "test-runner-config",
      "name": "Test Runner Config",
      "type": "library",
      "group": "test-execution",
      "description": "TestNG suite XML and Maven POM defining test execution order, dependency versions, and AspectJ weaving for Allure",
      "file": "src/test/resources/testng.xml",
      "patterns": [
        {
          "name": "suite-based-organization",
          "category": "testing"
        },
        {
          "name": "BOM-dependency-management",
          "category": "build"
        },
        {
          "name": "bytecode-weaving",
          "category": "build"
        }
      ]
    },
    {
      "id": "http-client",
      "name": "HTTP Client",
      "type": "library",
      "group": "support-libraries",
      "description": "Static utility class wrapping RestAssured into five HTTP method helpers (GET, POST, PUT, DELETE, PATCH) with JSON content type",
      "file": "src/test/java/bookstore/utils/RestAssuredUtils.java",
      "patterns": [
        {
          "name": "static-facade",
          "category": "structural"
        },
        {
          "name": "method-per-verb",
          "category": "api"
        }
      ]
    },
    {
      "id": "data-provider",
      "name": "Test Data Reader",
      "type": "library",
      "group": "support-libraries",
      "description": "Jackson-based JSON reader with two strategies: classpath loading for valid typed data and filesystem loading for invalid raw Map data",
      "file": "src/test/java/bookstore/data/JsonDataReader.java",
      "patterns": [
        {
          "name": "generic-deserialization",
          "category": "data"
        },
        {
          "name": "dual-path-loading",
          "category": "data"
        }
      ]
    },
    {
      "id": "domain-models",
      "name": "Domain Models",
      "type": "library",
      "group": "support-libraries",
      "description": "Lombok @Data POJOs representing Book and Author entities, mirroring the FakeRestAPI JSON schema",
      "file": "src/test/java/bookstore/model/",
      "patterns": [
        {
          "name": "Lombok-data",
          "category": "structural"
        }
      ]
    },
    {
      "id": "config-loader",
      "name": "Config Loader",
      "type": "library",
      "group": "support-libraries",
      "description": "Static properties reader that loads config.properties from classpath on class initialization and exposes values by key",
      "file": "src/test/java/bookstore/config/ConfigReader.java",
      "patterns": [
        {
          "name": "static-initializer",
          "category": "structural"
        },
        {
          "name": "fail-fast",
          "category": "resilience"
        }
      ]
    },
    {
      "id": "test-data-fixtures",
      "name": "Test Data Fixtures",
      "type": "store",
      "group": "test-data",
      "description": "JSON files providing valid entity data for positive tests and intentionally malformed data for negative validation tests",
      "file": "src/test/resources/data/"
    },
    {
      "id": "config-properties",
      "name": "Config Properties",
      "type": "store",
      "group": "test-data",
      "description": "Properties file holding the base URL of the system under test",
      "file": "src/test/resources/config.properties"
    },
    {
      "id": "ci-pipeline",
      "name": "CI Pipeline",
      "type": "service",
      "group": "ci-cd",
      "description": "GitHub Actions workflow that builds with Maven, runs all tests, generates Allure reports, and publishes them to gh-pages",
      "file": ".github/workflows/ci.yml",
      "patterns": [
        {
          "name": "build-test-report",
          "category": "ci"
        },
        {
          "name": "gh-pages-deployment",
          "category": "ci"
        },
        {
          "name": "continue-on-error",
          "category": "resilience"
        }
      ]
    },
    {
      "id": "fake-rest-api",
      "name": "FakeRestAPI",
      "type": "external",
      "group": "external",
      "description": "REST API hosted on Azure App Service providing Books and Authors endpoints that all tests validate against",
      "resilience": {
        "timeout": false,
        "retry": false,
        "circuitBreaker": false
      },
      "criticality": "critical"
    },
    {
      "id": "github-pages",
      "name": "GitHub Pages",
      "type": "external",
      "group": "external",
      "description": "Static site hosting for published Allure test reports on the gh-pages branch",
      "criticality": "optional"
    }
  ],
  "edges": [
    {
      "source": "test-suites",
      "target": "http-client",
      "label": "delegates HTTP calls",
      "style": "solid",
      "type": "depends_on"
    },
    {
      "source": "test-suites",
      "target": "data-provider",
      "label": "loads test data",
      "style": "solid",
      "type": "depends_on"
    },
    {
      "source": "test-suites",
      "target": "domain-models",
      "label": "uses POJOs",
      "style": "solid",
      "type": "depends_on"
    },
    {
      "source": "test-suites",
      "target": "config-loader",
      "label": "reads base URL",
      "style": "solid",
      "type": "depends_on"
    },
    {
      "source": "http-client",
      "target": "fake-rest-api",
      "label": "HTTP requests",
      "style": "dashed",
      "type": "data_flow"
    },
    {
      "source": "data-provider",
      "target": "test-data-fixtures",
      "label": "reads JSON fixtures",
      "style": "solid",
      "type": "depends_on"
    },
    {
      "source": "config-loader",
      "target": "config-properties",
      "label": "reads properties",
      "style": "solid",
      "type": "depends_on"
    },
    {
      "source": "ci-pipeline",
      "target": "test-suites",
      "label": "executes tests",
      "style": "solid",
      "type": "depends_on"
    },
    {
      "source": "ci-pipeline",
      "target": "test-runner-config",
      "label": "reads suite config",
      "style": "solid",
      "type": "depends_on"
    },
    {
      "source": "ci-pipeline",
      "target": "github-pages",
      "label": "publishes reports",
      "style": "dashed",
      "type": "data_flow"
    },
    {
      "source": "qa-engineer",
      "target": "test-suites",
      "label": "triggers mvn test",
      "style": "dashed",
      "type": "data_flow"
    },
    {
      "source": "ci-system",
      "target": "ci-pipeline",
      "label": "git push triggers",
      "style": "dashed",
      "type": "event"
    },
    {
      "source": "test-runner-config",
      "target": "test-suites",
      "label": "discovers test classes",
      "style": "solid",
      "type": "depends_on"
    }
  ],
  "data_flows": [
    {
      "id": "test-execution",
      "name": "Local Test Execution",
      "description": "QA engineer triggers Maven test run, which loads configuration, reads test data fixtures, sends HTTP requests to FakeRestAPI, and validates responses",
      "trigger": "mvn clean test (or IDE test runner)",
      "narrative": "A **QA Engineer** kicks off `mvn clean test`. Maven Surefire reads the suite definition in **Test Runner Config** and discovers **Test Suites** -- BookTests and AuthorTests.\n\nEach test class loads the base URL on first access. **Config Loader** reads `config.properties` from the classpath in a static initializer block, returning `https://fakerestapi.azurewebsites.net/`. If that file is missing, the class refuses to load -- fail-fast, no silent null.\n\n**Test Suites** call **Test Data Reader** through TestNG's `@DataProvider` mechanism. The reader has two paths: `readData` deserializes classpath JSON into typed `Book[]` or `Author[]` POJOs, while `readInvalidData` reads filesystem JSON into raw `List<Map>` -- intentionally untyped so the malformed payloads pass through without Jackson rejecting them.\n\nWith data in hand, each test constructs a URL and calls **HTTP Client**. RestAssuredUtils wraps RestAssured's fluent API into five static methods -- one per HTTP verb. The call hits **FakeRestAPI** on Azure, and the response comes back for assertion.\n\nTestNG's `assertEquals` checks status codes and body fields. Each step is wrapped in `Allure.step()` calls, so the report shows exactly what happened at each phase -- not just pass or fail, but the story of each request.",
      "narrative_map": [
        {
          "text": "A **QA Engineer** kicks off `mvn clean test`. Maven Surefire reads the suite definition in **Test Runner Config** and discovers **Test Suites** -- BookTests and AuthorTests.",
          "steps": [
            1,
            2
          ]
        },
        {
          "text": "Each test class loads the base URL on first access. **Config Loader** reads `config.properties` from the classpath in a static initializer block, returning `https://fakerestapi.azurewebsites.net/`. If that file is missing, the class refuses to load -- fail-fast, no silent null.",
          "steps": [
            3,
            4
          ]
        },
        {
          "text": "**Test Suites** call **Test Data Reader** through TestNG's `@DataProvider` mechanism. The reader has two paths: `readData` deserializes classpath JSON into typed `Book[]` or `Author[]` POJOs, while `readInvalidData` reads filesystem JSON into raw `List<Map>` -- intentionally untyped so the malformed payloads pass through without Jackson rejecting them.",
          "steps": [
            5,
            6
          ]
        },
        {
          "text": "With data in hand, each test constructs a URL and calls **HTTP Client**. RestAssuredUtils wraps RestAssured's fluent API into five static methods -- one per HTTP verb. The call hits **FakeRestAPI** on Azure, and the response comes back for assertion.",
          "steps": [
            7,
            8
          ]
        },
        {
          "text": "TestNG's `assertEquals` checks status codes and body fields. Each step is wrapped in `Allure.step()` calls, so the report shows exactly what happened at each phase -- not just pass or fail, but the story of each request.",
          "steps": [
            9
          ]
        }
      ],
      "steps": [
        {
          "component": "test-runner-config",
          "action": "Maven Surefire reads testng.xml and discovers test classes",
          "data": "suite definition (class names, test grouping)",
          "to": "test-suites",
          "technology": "Maven Surefire / TestNG"
        },
        {
          "component": "test-suites",
          "action": "test classes are instantiated and fields initialized",
          "data": "test class instances",
          "to": "test-suites",
          "technology": "TestNG"
        },
        {
          "component": "test-suites",
          "action": "static initialization loads base URL",
          "data": "config key 'base.url'",
          "to": "config-loader",
          "technology": "Java static field init"
        },
        {
          "component": "config-loader",
          "action": "reads config.properties from classpath and returns property value",
          "data": "base.url = https://fakerestapi.azurewebsites.net/",
          "to": "test-suites",
          "technology": "java.util.Properties"
        },
        {
          "component": "test-suites",
          "action": "DataProvider methods invoke JsonDataReader to load test fixtures",
          "data": "resource path (e.g. data/booksData.json)",
          "to": "data-provider",
          "technology": "TestNG @DataProvider callback"
        },
        {
          "component": "data-provider",
          "action": "deserializes JSON fixtures into POJOs or raw Maps",
          "data": "Book[], Author[], or List<Map<String,Object>>",
          "to": "test-suites",
          "technology": "Jackson ObjectMapper"
        },
        {
          "component": "test-suites",
          "action": "constructs full URL and delegates HTTP call with request body",
          "data": "URL + serialized entity (JSON)",
          "to": "http-client",
          "technology": "static method call"
        },
        {
          "component": "http-client",
          "action": "sends HTTP request to FakeRestAPI and returns response",
          "data": "HTTP request (GET/POST/PUT/DELETE/PATCH with JSON body)",
          "to": "fake-rest-api",
          "technology": "HTTP/JSON (RestAssured)"
        },
        {
          "component": "test-suites",
          "action": "asserts response status code and body fields against expected values",
          "data": "Response object (status code, JSON body)",
          "to": null,
          "technology": "TestNG assertEquals"
        }
      ]
    },
    {
      "id": "ci-execution",
      "name": "CI Pipeline Execution",
      "description": "GitHub Actions runs the full test suite on push, generates Allure reports with historical trends, and publishes to GitHub Pages",
      "trigger": "git push to any branch",
      "narrative": "A push lands on any branch and **GitHub Actions** wakes up. The workflow checks out the code and provisions JDK 21 (Zulu distribution) on an ubuntu-latest runner.\n\n**CI Pipeline** runs `mvn clean install`, which compiles the test code and hands off to Surefire. Every test in **Test Suites** executes -- the same flow as local execution, but unattended. The POM sets `testFailureIgnore=true`, so even if tests fail the build continues. That is deliberate: the point is to always produce a report, not to gate on green.\n\nAfter tests finish, the pipeline generates an Allure report. It fetches the previous report history from the gh-pages branch (with `continue-on-error`, because that branch may not exist yet on a fresh repo), merges in the new results, and publishes the combined HTML report to **GitHub Pages**. The team gets trend lines, step-level detail, and `@Issue` annotations -- all without opening an IDE.",
      "narrative_map": [
        {
          "text": "A push lands on any branch and **GitHub Actions** wakes up. The workflow checks out the code and provisions JDK 21 (Zulu distribution) on an ubuntu-latest runner.",
          "steps": [
            1
          ]
        },
        {
          "text": "**CI Pipeline** runs `mvn clean install`, which compiles the test code and hands off to Surefire. Every test in **Test Suites** executes -- the same flow as local execution, but unattended. The POM sets `testFailureIgnore=true`, so even if tests fail the build continues. That is deliberate: the point is to always produce a report, not to gate on green.",
          "steps": [
            2
          ]
        },
        {
          "text": "After tests finish, the pipeline generates an Allure report. It fetches the previous report history from the gh-pages branch (with `continue-on-error`, because that branch may not exist yet on a fresh repo), merges in the new results, and publishes the combined HTML report to **GitHub Pages**. The team gets trend lines, step-level detail, and `@Issue` annotations -- all without opening an IDE.",
          "steps": [
            3,
            4
          ]
        }
      ],
      "steps": [
        {
          "component": "ci-pipeline",
          "action": "checks out code and sets up JDK 21 (Zulu)",
          "data": "source code + JDK runtime",
          "to": "test-runner-config",
          "technology": "GitHub Actions (actions/checkout, setup-java)"
        },
        {
          "component": "ci-pipeline",
          "action": "runs mvn clean install which compiles and executes all tests",
          "data": "Maven build lifecycle",
          "to": "test-suites",
          "technology": "Maven Surefire"
        },
        {
          "component": "ci-pipeline",
          "action": "generates Allure report from test results",
          "data": "allure-results directory",
          "to": null,
          "technology": "allure-maven plugin + allure-report-action"
        },
        {
          "component": "ci-pipeline",
          "action": "publishes report to gh-pages branch",
          "data": "HTML report artifacts",
          "to": "github-pages",
          "technology": "peaceiris/actions-gh-pages"
        }
      ]
    }
  ],
  "state": [
    {
      "id": "test-data-json",
      "name": "Test Fixtures",
      "description": "Valid and invalid entity payloads for Books and Authors (1 valid book, 4 invalid books, 2 valid authors, 4 invalid authors)",
      "purpose": "source-of-truth",
      "technology": "JSON files on filesystem",
      "component": "test-data-fixtures",
      "persistence": "persistent",
      "narrative": "Test fixtures are the single source of truth for what data flows into every data-driven test. Four JSON files split along two axes: entity type (books vs. authors) and validity (well-formed POJOs vs. intentionally broken payloads).\n\nThe valid files (`booksData.json`, `authorsData.json`) contain typed entities that Jackson deserializes into `Book` and `Author` POJOs. The invalid files (`booksInvalidData.json`, `authorsInvalidData.json`) use wrong types on purpose -- strings where integers belong -- and **Test Data Reader** loads them as raw `Map<String, Object>` to bypass Jackson's type checking. This dual-path approach means negative tests send genuinely malformed JSON, not Java objects that happen to have null fields.\n\nAdding a new test scenario is a JSON edit, not a code change. The `@DataProvider` methods iterate the array automatically.",
      "narrative_map": [
        {
          "text": "Test fixtures are the single source of truth for what data flows into every data-driven test. Four JSON files split along two axes: entity type (books vs. authors) and validity (well-formed POJOs vs. intentionally broken payloads).",
          "refs": [
            "test-data-fixtures"
          ]
        },
        {
          "text": "The valid files (`booksData.json`, `authorsData.json`) contain typed entities that Jackson deserializes into `Book` and `Author` POJOs. The invalid files (`booksInvalidData.json`, `authorsInvalidData.json`) use wrong types on purpose -- strings where integers belong -- and **Test Data Reader** loads them as raw `Map<String, Object>` to bypass Jackson's type checking. This dual-path approach means negative tests send genuinely malformed JSON, not Java objects that happen to have null fields.",
          "refs": [
            "test-data-fixtures",
            "data-provider",
            "domain-models"
          ]
        },
        {
          "text": "Adding a new test scenario is a JSON edit, not a code change. The `@DataProvider` methods iterate the array automatically.",
          "refs": [
            "test-suites",
            "test-data-fixtures"
          ]
        }
      ],
      "readers": [
        "data-provider"
      ],
      "writers": [
        "qa-engineer"
      ]
    },
    {
      "id": "config-props",
      "name": "Application Config",
      "description": "Base URL of the system under test",
      "purpose": "source-of-truth",
      "technology": "Java .properties file on classpath",
      "component": "config-properties",
      "persistence": "persistent",
      "narrative": "A single properties file holds the base URL for the **FakeRestAPI**. **Config Loader** reads it once, in a static initializer block, and caches it for the lifetime of the JVM. If the file is missing, the static block throws -- no test will execute with a silently null URL.\n\nThis is the only environment-specific value in the entire framework. Switching the suite to target a different API instance is a one-line edit in `config.properties`.",
      "narrative_map": [
        {
          "text": "A single properties file holds the base URL for the **FakeRestAPI**. **Config Loader** reads it once, in a static initializer block, and caches it for the lifetime of the JVM. If the file is missing, the static block throws -- no test will execute with a silently null URL.",
          "refs": [
            "config-properties",
            "config-loader",
            "fake-rest-api"
          ]
        },
        {
          "text": "This is the only environment-specific value in the entire framework. Switching the suite to target a different API instance is a one-line edit in `config.properties`.",
          "refs": [
            "config-properties"
          ]
        }
      ],
      "readers": [
        "config-loader"
      ],
      "writers": [
        "qa-engineer"
      ]
    },
    {
      "id": "allure-results",
      "name": "Allure Results",
      "description": "Per-test execution records with steps, descriptions, issues, and timing",
      "purpose": "derived",
      "technology": "JSON result files in target/allure-results",
      "component": "ci-pipeline",
      "persistence": "ephemeral",
      "narrative": "Every test method produces a JSON result file in `target/allure-results/`. These files capture the step-by-step execution trace -- each `Allure.step()` call in **Test Suites** becomes a named entry with timing data.\n\nThe results are ephemeral. They exist only between the test run and the report generation step. The CI Pipeline Execution flow consumes them to produce the final Allure HTML report on **GitHub Pages**.",
      "narrative_map": [
        {
          "text": "Every test method produces a JSON result file in `target/allure-results/`. These files capture the step-by-step execution trace -- each `Allure.step()` call in **Test Suites** becomes a named entry with timing data.",
          "refs": [
            "ci-pipeline",
            "test-suites"
          ]
        },
        {
          "text": "The results are ephemeral. They exist only between the test run and the report generation step. The CI Pipeline Execution flow consumes them to produce the final Allure HTML report on **GitHub Pages**.",
          "refs": [
            "ci-pipeline",
            "github-pages"
          ]
        }
      ],
      "readers": [
        "ci-pipeline"
      ],
      "writers": [
        "test-suites"
      ]
    },
    {
      "id": "allure-report",
      "name": "Allure Report",
      "description": "Rendered Allure HTML report with historical trends on gh-pages",
      "purpose": "derived",
      "technology": "Static HTML site on gh-pages branch",
      "component": "github-pages",
      "persistence": "persistent",
      "narrative": "The final artifact of every CI run: a static HTML site on the gh-pages branch. It accumulates history across runs, showing trend lines for pass rates and timing.\n\n`@Issue` annotations from **Test Suites** surface here as linked tags, so the team can distinguish genuine regressions from known **FakeRestAPI** quirks (like DELETE returning 200 for nonexistent IDs). The report is the team's primary feedback channel -- more useful than raw Maven output because it shows the why, not just the what.",
      "narrative_map": [
        {
          "text": "The final artifact of every CI run: a static HTML site on the gh-pages branch. It accumulates history across runs, showing trend lines for pass rates and timing.",
          "refs": [
            "github-pages",
            "ci-pipeline"
          ]
        },
        {
          "text": "`@Issue` annotations from **Test Suites** surface here as linked tags, so the team can distinguish genuine regressions from known **FakeRestAPI** quirks (like DELETE returning 200 for nonexistent IDs). The report is the team's primary feedback channel -- more useful than raw Maven output because it shows the why, not just the what.",
          "refs": [
            "test-suites",
            "fake-rest-api",
            "github-pages"
          ]
        }
      ],
      "readers": [
        "qa-engineer"
      ],
      "writers": [
        "ci-pipeline"
      ]
    }
  ],
  "failure_modes": [
    {
      "id": "sut-unavailable",
      "trigger": "FakeRestAPI at azurewebsites.net is down, returns 5xx, or is unreachable",
      "severity": "critical",
      "impact": "Entire test suite produces false negatives -- no valid regression signal",
      "narrative": "At any hour, the Azure-hosted **FakeRestAPI** starts returning 503s or drops connections entirely. **HTTP Client** is a thin facade with no timeout, retry, or circuit breaker -- RestAssured throws a `ConnectionException` or the response comes back with an unexpected status code.\n\nAll 23 tests in **Test Suites** fail with assertion errors. But the build still passes -- `testFailureIgnore=true` in the POM means **CI Pipeline** exits 0 regardless. The Allure report on **GitHub Pages** shows a wall of red, but there is no alert. Someone has to look.\n\nThis is the framework's biggest blind spot. There is no smoke test or health-check step before the full suite. A single curl to the API's index page would distinguish \"API is down\" from \"our tests have a bug.\" Until that guard exists, a 100% failure report is ambiguous.",
      "narrative_map": [
        {
          "text": "At any hour, the Azure-hosted **FakeRestAPI** starts returning 503s or drops connections entirely. **HTTP Client** is a thin facade with no timeout, retry, or circuit breaker -- RestAssured throws a `ConnectionException` or the response comes back with an unexpected status code.",
          "cascade_steps": [
            1
          ],
          "refs": [
            "fake-rest-api",
            "http-client"
          ]
        },
        {
          "text": "All 23 tests in **Test Suites** fail with assertion errors. But the build still passes -- `testFailureIgnore=true` in the POM means **CI Pipeline** exits 0 regardless. The Allure report on **GitHub Pages** shows a wall of red, but there is no alert. Someone has to look.",
          "cascade_steps": [
            2,
            3
          ],
          "refs": [
            "test-suites",
            "ci-pipeline",
            "github-pages"
          ]
        },
        {
          "text": "This is the framework's biggest blind spot. There is no smoke test or health-check step before the full suite. A single curl to the API's index page would distinguish \"API is down\" from \"our tests have a bug.\" Until that guard exists, a 100% failure report is ambiguous.",
          "refs": [
            "ci-pipeline"
          ]
        }
      ],
      "cascade": [
        {
          "component": "http-client",
          "effect": "RestAssured requests throw ConnectionException or return unexpected status codes"
        },
        {
          "component": "test-suites",
          "effect": "all 23 tests fail with assertion errors or connection timeouts"
        },
        {
          "component": "ci-pipeline",
          "effect": "CI job completes (testFailureIgnore=true) but Allure report shows full failure"
        }
      ],
      "detection": [
        "Allure report shows 100% failure rate",
        "CI job succeeds (exit 0 due to testFailureIgnore) but all tests are red",
        "Connection timeout errors in Maven surefire output"
      ],
      "recovery": [
        "Verify FakeRestAPI health at https://fakerestapi.azurewebsites.net/index.html",
        "Re-trigger CI pipeline once API is restored",
        "Consider adding a smoke test or health-check step before full suite"
      ]
    },
    {
      "id": "test-data-missing",
      "trigger": "JSON test data files are deleted, renamed, or contain malformed JSON",
      "severity": "high",
      "impact": "Data-driven tests cannot execute -- 14 of 23 tests affected",
      "narrative": "Someone renames `booksData.json` or introduces a trailing comma. **Test Data Reader** tries to load the fixture and Jackson throws -- either `FileNotFoundException` wrapped in a `RuntimeException`, or a `JsonParseException` from malformed syntax.\n\nThe failure hits at DataProvider initialization, before any HTTP call fires. TestNG marks every data-driven test method as failed -- that is 14 of 23 tests across both BookTests and AuthorTests. The remaining 9 tests (static ones like `getAllBooks`, `getBookById`, `patchBook`) still run normally.\n\nRecovery is a `git checkout -- src/test/resources/data/`. A JSON linter in the pre-commit hook would catch this before it reaches CI -- see the SUT Unavailable failure for why CI alone does not surface the difference clearly.",
      "narrative_map": [
        {
          "text": "Someone renames `booksData.json` or introduces a trailing comma. **Test Data Reader** tries to load the fixture and Jackson throws -- either `FileNotFoundException` wrapped in a `RuntimeException`, or a `JsonParseException` from malformed syntax.",
          "cascade_steps": [
            1
          ],
          "refs": [
            "data-provider",
            "test-data-fixtures"
          ]
        },
        {
          "text": "The failure hits at DataProvider initialization, before any HTTP call fires. TestNG marks every data-driven test method as failed -- that is 14 of 23 tests across both BookTests and AuthorTests. The remaining 9 tests (static ones like `getAllBooks`, `getBookById`, `patchBook`) still run normally.",
          "cascade_steps": [
            2
          ],
          "refs": [
            "test-suites"
          ]
        },
        {
          "text": "Recovery is a `git checkout -- src/test/resources/data/`. A JSON linter in the pre-commit hook would catch this before it reaches CI -- see the SUT Unavailable failure for why CI alone does not surface the difference clearly.",
          "refs": [
            "test-data-fixtures",
            "ci-pipeline"
          ]
        }
      ],
      "cascade": [
        {
          "component": "data-provider",
          "effect": "JsonDataReader throws RuntimeException wrapping FileNotFoundException or JsonParseException"
        },
        {
          "component": "test-suites",
          "effect": "DataProvider-backed tests fail at data loading before any HTTP call -- 14 of 23 tests affected"
        }
      ],
      "detection": [
        "RuntimeException: Failed to read JSON data in test output",
        "TestNG reports DataProvider initialization failure"
      ],
      "recovery": [
        "Restore JSON files from git (git checkout -- src/test/resources/data/)",
        "Validate JSON syntax with a linter before commit"
      ]
    },
    {
      "id": "config-missing",
      "trigger": "config.properties file is missing from classpath or base.url key is absent",
      "severity": "critical",
      "impact": "Entire suite is dead on arrival -- zero tests execute",
      "narrative": "**Config Loader** uses a static initializer block. If `config.properties` is not on the classpath, the block throws `RuntimeException(\"config.properties not found in resources folder\")`. The JVM wraps this in an `ExceptionInInitializerError`.\n\nBoth `BookTests` and `AuthorTests` reference `ConfigReader.get(\"base.url\")` as a field initializer. When ConfigReader fails to load, every test class that touches it also fails to load. Zero tests run. The Allure report is empty -- not red, empty.\n\nThis is actually well-designed. A missing config file is a broken environment, not a test failure. Fail-fast surfaces the problem immediately instead of producing 23 confusing NullPointerExceptions.",
      "narrative_map": [
        {
          "text": "**Config Loader** uses a static initializer block. If `config.properties` is not on the classpath, the block throws `RuntimeException(\"config.properties not found in resources folder\")`. The JVM wraps this in an `ExceptionInInitializerError`.",
          "cascade_steps": [
            1
          ],
          "refs": [
            "config-loader"
          ]
        },
        {
          "text": "Both `BookTests` and `AuthorTests` reference `ConfigReader.get(\"base.url\")` as a field initializer. When ConfigReader fails to load, every test class that touches it also fails to load. Zero tests run. The Allure report is empty -- not red, empty.",
          "cascade_steps": [
            2
          ],
          "refs": [
            "test-suites",
            "config-loader"
          ]
        },
        {
          "text": "This is actually well-designed. A missing config file is a broken environment, not a test failure. Fail-fast surfaces the problem immediately instead of producing 23 confusing NullPointerExceptions.",
          "refs": [
            "config-loader"
          ]
        }
      ],
      "cascade": [
        {
          "component": "config-loader",
          "effect": "static initializer throws RuntimeException and class fails to load"
        },
        {
          "component": "test-suites",
          "effect": "all test classes fail at field initialization since they call ConfigReader.get('base.url')"
        }
      ],
      "detection": [
        "ExceptionInInitializerError in test output",
        "RuntimeException: config.properties not found in resources folder"
      ],
      "recovery": [
        "Restore config.properties from git",
        "Verify file is at src/test/resources/config.properties and contains base.url key"
      ]
    },
    {
      "id": "sut-behavior-regression",
      "trigger": "FakeRestAPI changes response codes or body schema",
      "severity": "medium",
      "impact": "Test failures that may be true regressions or expected API quirks",
      "narrative": "**FakeRestAPI** is a third-party mock API. Its behavior drifts. Known example: DELETE with a nonexistent ID returns 200 instead of 404. PUT with an invalid ID returns the wrong status code. **Test Suites** tag these with `@Issue` annotations -- a deliberate record that the test expects standard behavior but the API deviates.\n\nWhen these tests fail, the Allure report shows the `@Issue` tag so the reviewer knows this is a documented quirk, not a new bug. But if the API changes its behavior again -- fixing the quirk, or introducing a new one -- the `@Issue` annotation becomes stale.\n\nThe distinction matters: a test that fails with an `@Issue` tag is \"known flaky.\" A test that fails without one is \"investigate immediately.\" Keeping those tags current is a manual discipline, not an automated check.",
      "narrative_map": [
        {
          "text": "**FakeRestAPI** is a third-party mock API. Its behavior drifts. Known example: DELETE with a nonexistent ID returns 200 instead of 404. PUT with an invalid ID returns the wrong status code. **Test Suites** tag these with `@Issue` annotations -- a deliberate record that the test expects standard behavior but the API deviates.",
          "cascade_steps": [
            1
          ],
          "refs": [
            "fake-rest-api",
            "test-suites"
          ]
        },
        {
          "text": "When these tests fail, the Allure report shows the `@Issue` tag so the reviewer knows this is a documented quirk, not a new bug. But if the API changes its behavior again -- fixing the quirk, or introducing a new one -- the `@Issue` annotation becomes stale.",
          "refs": [
            "test-suites",
            "github-pages"
          ]
        },
        {
          "text": "The distinction matters: a test that fails with an `@Issue` tag is \"known flaky.\" A test that fails without one is \"investigate immediately.\" Keeping those tags current is a manual discipline, not an automated check.",
          "refs": [
            "test-suites"
          ]
        }
      ],
      "cascade": [
        {
          "component": "test-suites",
          "effect": "assertion failures on status code or body field checks"
        }
      ],
      "detection": [
        "Specific test failures in deleteBookWithInvalidId, updateBookWithInvalidId, deleteAuthorWithInvalidId, updateAuthorWithInvalidId",
        "@Issue annotations in Allure report flag known discrepancies"
      ],
      "recovery": [
        "Compare actual vs expected behavior against API documentation",
        "Update @Issue annotations or expected values if API behavior is intentional"
      ]
    }
  ],
  "overview": "Bookstore-APIs is a test automation framework -- not an application. It validates a third-party Bookstore REST API using 23 data-driven tests across Books and Authors endpoints, producing Allure reports with step-level traceability on every push.",
  "structure_narrative": "## Three Boundaries\n\nThe framework operates across three boundaries: the test execution layer where tests live and run, the support libraries that handle HTTP, data, config, and domain modeling, and the external world -- a single Azure-hosted API and GitHub's CI/Pages infrastructure.\n\n## Test Execution\n\n**Test Suites** is the center of gravity. BookTests (11 methods) and AuthorTests (12 methods) cover every CRUD verb plus negative cases -- invalid IDs, malformed payloads, unsupported methods. **Test Runner Config** (testng.xml plus the Maven POM) defines suite composition and wires in Allure's AspectJ agent for step-level reporting.\n\n## Support Stack\n\nFour support libraries sit beneath the tests, each with a single job. **HTTP Client** wraps RestAssured into five static methods -- one per HTTP verb -- so tests never construct requests directly. **Test Data Reader** loads JSON fixtures through two paths: classpath for typed POJOs, filesystem for raw Maps (the dual-path design ensures negative tests send genuinely broken JSON). **Domain Models** are Lombok `@Data` classes mirroring the API schema. **Config Loader** reads the base URL once, in a static block, and fails hard if the file is missing.\n\n## Data and CI\n\n**Test Data Fixtures** hold the JSON files that feed every `@DataProvider`. **CI Pipeline** ties it together -- GitHub Actions runs `mvn clean install` on every push, generates an Allure report, and publishes it to **GitHub Pages**. The pipeline deliberately does not fail on test failures (`testFailureIgnore=true`) because the report is the deliverable, not a green build.",
  "structure_narrative_map": [
    {
      "text": "## Three Boundaries\n\nThe framework operates across three boundaries: the test execution layer where tests live and run, the support libraries that handle HTTP, data, config, and domain modeling, and the external world -- a single Azure-hosted API and GitHub's CI/Pages infrastructure.",
      "refs": [
        "test-suites",
        "http-client",
        "data-provider",
        "config-loader",
        "domain-models",
        "fake-rest-api",
        "ci-pipeline",
        "github-pages"
      ]
    },
    {
      "text": "## Test Execution\n\n**Test Suites** is the center of gravity. BookTests (11 methods) and AuthorTests (12 methods) cover every CRUD verb plus negative cases -- invalid IDs, malformed payloads, unsupported methods. **Test Runner Config** (testng.xml plus the Maven POM) defines suite composition and wires in Allure's AspectJ agent for step-level reporting.",
      "refs": [
        "test-suites",
        "test-runner-config"
      ]
    },
    {
      "text": "## Support Stack\n\nFour support libraries sit beneath the tests, each with a single job. **HTTP Client** wraps RestAssured into five static methods -- one per HTTP verb -- so tests never construct requests directly. **Test Data Reader** loads JSON fixtures through two paths: classpath for typed POJOs, filesystem for raw Maps (the dual-path design ensures negative tests send genuinely broken JSON). **Domain Models** are Lombok `@Data` classes mirroring the API schema. **Config Loader** reads the base URL once, in a static block, and fails hard if the file is missing.",
      "refs": [
        "http-client",
        "data-provider",
        "domain-models",
        "config-loader"
      ]
    },
    {
      "text": "## Data and CI\n\n**Test Data Fixtures** hold the JSON files that feed every `@DataProvider`. **CI Pipeline** ties it together -- GitHub Actions runs `mvn clean install` on every push, generates an Allure report, and publishes it to **GitHub Pages**. The pipeline deliberately does not fail on test failures (`testFailureIgnore=true`) because the report is the deliverable, not a green build.",
      "refs": [
        "test-data-fixtures",
        "ci-pipeline",
        "github-pages"
      ]
    }
  ]
}